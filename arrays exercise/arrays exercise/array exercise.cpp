#include <iostream>
int main()
{
	//int values[] = { 2, 6, 10, 14};
	int arr[3][3];
	//00
	//01
	//02
	//10
	//11
	//12
	//20
	//21
	//22
	for (int i = 0; i < 3; i++)
	{
		for (int i = 0; i < 3; i++)
		{

		}
	}
	system("pause");
}
// #1
	//int numbers[10] = { 0, 0, 1, 0, 0, 1, 0, 0, 1, 1 };/* valid definition*/
	//int matrix[5] = { 1, 2, 3, 4, 5, 6, 7 }; /*invalid: the numbers go out of range*/
	//double radii[10] = { 3.2, 4.7 }; /* valid definition*/
	//int table[7] = { 2, , , 27, , 45, 39 };/* invalid: there must be a number between each comma*/
	//char codes[] = { 'A', 'X', '1', '2', 's' }; //invalid because this doesnt define the spots in the array
	//int blank[]	/*in valid because the array has to be at least initialized if there is no set size*/
	//int collection[-20]; /*invalid because of negative size*/
	//int hours[3] = 8, 12, 16; //invalid: must be defined inside of a scope
// #2

	//(a) cout << values[2] displays 10*
	//(b)cout << ++values[0]; //3 adds 1 to info at index 0
	//(c)cout << values[1]++; //prints 6
	//(D)x = 2;
	//cout << values[++x]; //nothing because x is undefined
	//(e)std::cout << values[4]; memory address because nothing exists at that index

//3
	//int data[10];
	//int num = 10;
	//for (int = i; i < 10; i++)
	//{
	//	data[10] = num;
	//	num--;
	//}

//4 
	/*int nums[5];
	for (int i = 0; i < 5; i++)
	{
		std::cin >> nums[i];
	}
	for(int i = 4; i < 0; i++)
	{
	std::cout << nums[i]
	}
	*/
	

//5

	//int value[10];
	//int mostemp = 0;
	//for (int i = 0; i < 10; i++)
	//{
	//	std::cin >> value[i];

	//	if (value[i] > mostemp)
	//	{
	//		mostemp = value[i];
	//	}
	//	
	//}
	//int lesstemp = mostemp;
	//for (int i = 0; i < 10; i++)
	//{
	//	if (value[i] < lesstemp)
	//	{
	//		lesstemp = value[i];
	//	}
	//}

//6

